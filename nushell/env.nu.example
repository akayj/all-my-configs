# env.nu - 用户特定的环境变量配置
# 将此文件复制为 ~/.config/nushell/env.nu 并根据需要修改
# 此文件不会被版本控制，可以包含敏感信息

# 示例环境变量
# $env.GITHUB_TOKEN = "your-github-token-here"
# $env.API_KEY = "your-api-key-here"

# 其他自定义环境变量
# $env.CUSTOM_VAR = "custom-value"

# 注意：此文件中的变量会覆盖 config.nu 中的同名变量